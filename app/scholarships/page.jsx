'use client';
import Portal from '../components/Portal'; import ScholarCard from '../components/ScholarCard'; import {scholarships} from '../lib/scholarships'; import {useMemo,useState} from 'react';
export default function Scholarships(){const [query,setQuery]=useState('');const [type,setType]=useState('');const [level,setLevel]=useState('');const shown=useMemo(()=>scholarships.filter(s=>`${s.title} ${s.provider} ${s.type} ${s.field}`.toLowerCase().includes(query.toLowerCase())&&(!type||s.type===type)&&(!level||s.level===level)),[query,type,level]);return <Portal active="Find Scholarships"><div className="page"><section className="hero-banner"><img src="/assets/explore-banner.jpeg" alt="Explore scholarships"/><div className="hero-content"><h1>Explore Scholarships</h1><p>Discover opportunities that match your dreams. Search, filter and find the best scholarships for your future.</p><div className="landing-search"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search by scholarship, provider or keyword..."/><button>Search</button></div></div></section><div className="filters-layout"><section><div className="section-head"><h2>Showing {shown.length} scholarships</h2></div><div className="scholar-grid">{shown.map(s=><ScholarCard scholarship={s} key={s.id}/>)}</div>{!shown.length&&<div className="empty">No scholarships found. Try resetting a filter.</div>}</section><aside className="filter-card panel"><h3>⌕ Filter Scholarships</h3><label>Education Level<select value={level} onChange={e=>setLevel(e.target.value)}><option value="">All education levels</option><option>Undergraduate</option><option>Postgraduate</option></select></label><label>Scholarship Type<select value={type} onChange={e=>setType(e.target.value)}><option value="">All scholarship types</option><option>Merit-based</option><option>Need-based</option><option>Government</option></select></label><button className="primary" onClick={()=>{setType('');setLevel('');setQuery('')}}>Reset Filters</button></aside></div></div></Portal>}
