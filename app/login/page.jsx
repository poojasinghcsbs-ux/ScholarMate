'use client';
import Link from 'next/link'; import {useState} from 'react';
export default function Login(){const [message,setMessage]=useState('');const submit=e=>{e.preventDefault();const email=e.currentTarget.email.value;const account=JSON.parse(localStorage.getItem('scholarMateAccount')||'null');if(!account||account.email!==email){setMessage('No account found. Please create an account first.');return}localStorage.setItem('scholarMateSession',JSON.stringify({email}));window.location.href='/dashboard'};return <main className="auth"><section className="auth-art"><img src="/assets/study-bg.jpeg" alt=""/><div className="portal-brand"><span>🎓</span><b>ScholarMate<small>Learn Today, Lead Tomorrow</small></b></div><h1>Every dream deserves a direction.</h1></section><section className="auth-panel"><form className="auth-form" onSubmit={submit}><Link href="/home">← Back to home</Link><h1>Welcome back</h1><p className="subcopy">Log in to continue your scholarship journey.</p><label className="field">Email address<input required name="email" type="email" placeholder="you@example.com"/></label><label className="field">Password<input required name="password" type="password" placeholder="Enter your password"/></label>{message&&<p className="form-note">{message}</p>}<button className="primary" type="submit">Log In →</button><p className="form-note">New to ScholarMate? <Link href="/signup">Create an account</Link></p></form></section></main>}
