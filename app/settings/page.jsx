'use client';
import Portal,{useStudent} from '../components/Portal';import {useEffect,useState} from 'react';
export default function Settings(){const {student,signedIn}=useStudent();const [name,setName]=useState('');const [email,setEmail]=useState('');const [saved,setSaved]=useState(false);useEffect(()=>{setName(student.fullName||'');setEmail(student.email||'')},[student]);const save=()=>{const profile={...student,fullName:name,email};localStorage.setItem('scholarMateProfile',JSON.stringify(profile));setSaved(true);setTimeout(()=>setSaved(false),2200)};return <Portal active="Settings"><div className="page"><h1 className="page-title">Settings</h1><p className="subcopy">Manage your account and notification preferences.</p>{!signedIn?<section className="empty"><h2>Log in to manage settings</h2><p>Account preferences are available only after login.</p></section>:<section className="settings-layout"><aside className="settings-menu panel"><button className="active">♙ Account</button><button>♧ Notifications</button><button>⚙ Preferences</button></aside><section className="settings-panel panel"><h2>Account Details</h2><label className="field">Full name<input value={name} onChange={e=>setName(e.target.value)}/></label><label className="field">Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><h2>Notification Preferences</h2><p className="subcopy">Deadline reminders, scholarship matches and application updates are currently set to on.</p><button className="primary" onClick={save}>Save Changes →</button>{saved&&<p className="form-note">Settings saved.</p>}</section></section>}</div></Portal>}
